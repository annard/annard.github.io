(setq hyde/git/remote "origin" ; The name of the remote to which we should push
      hyde/git/remote "master" ; The name of the branch on which your blog resides
      hyde/jekyll-command "rvm use 2.6.3@jekyll && bundle exec jekyll build"
      hyde/jekyll-serve "rvm use 2.6.3@jekyll && bundle exec jekyll serve"
      hyde-custom-params '(("category" "boat"))
      )
